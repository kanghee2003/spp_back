<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
  <property name="LOG_PATTERN" value="%boldGreen(%d{yy-MM-dd HH:mm:ss.SSS}) [%thread] [%highlight(%-5level)] %magenta(%-4relative) [%boldBlue(%logger{0}:%line)] - %boldYellow(%msg%n)"/>
  <!-- set by application-{profile}.yml -> app.logging.file-path -->
  <property name="LOG_PATH" value="${app.logging.file-path:-./logs}"/>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/backend-dev.log</file>
    <append>true</append>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/backend-dev.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
      <maxHistory>30</maxHistory>
      <totalSizeCap>2GB</totalSizeCap>
    </rollingPolicy>

    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <logger name="p6spy" level="INFO"/>
  <logger name="com.shinhan.spp" level="INFO"/>

  <root level="INFO">
    <appender-ref ref="FILE"/>
  </root>
</configuration>
